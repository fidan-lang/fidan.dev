---
title: "Tools"
sidebarLabel: "Overview"
description: "The CLI, formatter, diagnostics, replay tooling, language server, and editor integration are part of the Fidan product, not optional afterthoughts."
summary: "A language experience shaped as a toolchain, not just a compiler binary."
order: 300
---

# Tools

Fidan is not only a language grammar and a code generator.

The tooling story is part of why the language feels coherent:

- one CLI front door
- one formatter path shared by the CLI and the editor
- one diagnostics system with documented codes
- one language server that understands builtins, decorators, stdlib metadata,
  return types, and unreachable code
- replay and tracing flows that make failures easier to trust
