---
title: "libfidan embedding"
sidebarLabel: "libfidan embedding"
description: "Embed Fidan into native hosts with the dedicated library artifacts instead of treating the CLI as the only integration story."
summary: "A language should be embeddable where that makes architectural sense."
order: 470
---

# libfidan embedding

Fidan is not only about the CLI. It also has an embedding story through
`libfidan`.

## Why embedding matters

If a language wants to participate in real native products, it needs a path
into larger systems too.
